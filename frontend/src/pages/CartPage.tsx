export function CartPage() {
  return (
    <section className="card">
      <h2>Cart</h2>
      <p>Cart state is intentionally lightweight for portfolio demo. Add items from catalog in backend-connected iterations.</p>
    </section>
  )
}
